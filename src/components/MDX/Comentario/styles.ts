import styled from 'styled-components';

export const Comment = styled.span`
  color: #71717a;
  font-family: 'Fira Code', monospace;
  font-size: 0.9rem;
  font-style: italic;
  display: block;
  margin-top: 0.5rem;
`;