# Atividade 03 - Parte 01

## VM1_INTERNET

- dhclient eth1 (se não funcionar, veja se o adaptador de rede está como "1". se mesmo assim não der certo, tente `dhclient eth1 -r`)
- ssh seed@10.3.150.90
- yes
- dees

## VM2_GATEWAY

- iptables -t nat -A POSTROUTING -j MASQUERADE

## VM3_LOCAL_INTERNET

Vá na VM_U26s e dê `ifconfig` para pegar o IP referente ao `enp0s3` (192.168.56.102).

se não der certo, vá na VM2_GATEWAY e coloque o `dhclient eth1` novamente. depois tente o `ssh` na VM3_LOCAL_INTERNET.

"Are you sure you want to continue connectting (yes/no) ?" `yes`
- senha: 654123
- sudo su

## VM2_GATEWAY

iptables -t nat -A POSTROUTING -j MASQUERADE

## VM3_LOCAL_INTERNET

route add -net 10.3.150.90 (IP_SEED) netmask 255.255.255.0 gw (`eth0` da VM2_GATEWAY) 10.3.150.68

se der erro de "route: netmask doesn't match route address": route add -net 10.3.150.0 netmask 255.255.255.0 gw 192.168.56.102 (IP_U26s)